{
  "name": "novacart-ai",
  "version": "1.0.0",
  "description": "NovaCart AI - Full Stack E-Commerce Platform",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "dev": "cd frontend && npm run dev"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
